# 프로젝트 현황판

> 새 세션 시작 시 이 파일 하나만 읽고 시작한다. 상황이 바뀌면 해당 항목만 갱신하고 마지막 갱신일을 기록한다.

마지막 갱신: 2026-09-01

## 진행 중

### 마켓빅시 BIGSEE — 신규
- 상태: 2026-08-13 공개 페이지 기반 서비스·사업성 분석, 대행사 우선 판매·경영 전략, 이커머스 마케팅 퍼널, 블로그 자동 작성·SKU 성과 측정·외부 API 연동 기능 기획을 `market-bigsee/`에 정리. 정본 시작점은 `market-bigsee/README.md`
- 제품 방향: `시장 조사 → 콘텐츠 기획·초안 → SKU·캠페인 연결 → 유입·구매 측정 → 다음 실행 추천`. 블로그 기능은 범용 자동 작문이 아니라 조사 근거·사람 승인·SKU·매출 추적을 결합하고, 허위 리뷰 생성은 제외
- MVP 제안: SKU 직접/CSV 등록 → 조사 기반 블로그 초안 → 사람 승인 → 추적 링크 → 주문 CSV → 기본 성과판. 이후 GA4와 스마트스토어 또는 카페24 한 곳, 팀 기능, 공개 API 순서
- 미확인: 실제 코드 구조, 사용자·플랫폼·AI별 원가, 서버 측 사용량 계측, 팀/권한 구조, 외부 API 계약·쿼터, 고객별 주 사용 쇼핑몰
- 다음: 첫 고객군과 첫 커머스 연동 선택 → 현재 코드 저장소 점검 → 파일럿 대행사 3곳으로 MVP 흐름 검증

### 산후조리원 고도몰 (아이베)
- 상태: 2026-08-06 라이브 오픈 완료 — 운영 모니터링 단계
- 다음: 라이브 QA — 장바구니 혼합 차단, 뱅크다 무통장 자동매칭, 등급별 결제 권한 (`산후조리원/project_checklist.md`)

### 워크스페이스 인프라
- 상태: 2026-08-15 **하네스 4계층 재편 (L0 미설치).** L0 강제(훅) / L1 헌법(`AGENTS.md`) / L2 지식(`.claude/skills/` 9개, ASCII 슬러그) / L3 상태(이 파일). 규격 정본 `harness/SPEC.md`, 정합성 검사기 `harness/check.py`
- 상시 로드 597줄 → **134줄(측정 기준: `harness/check.py`)**. 단 `(재시작 후 실측 필요)` — 세션 시작 스냅샷 때문에 개편한 세션에서는 구 룰이 그대로 주입된다. 다음 세션에서 `/context`로 확인하고 이 줄을 정정한다
- 근거: 상시 로드 비용은 캐싱이 90% 상쇄하나 **준수율은 분량에 반비례**한다(지시 150~200개 한계, 시스템 프롬프트가 이미 ~50개 사용)
- 멀티 도구: 루트 `AGENTS.md` ← `CLAUDE.md`가 `@AGENTS.md` 임포트. 스킬 탐색 경로는 도구마다 달라 Claude Code·Cursor는 `.claude/skills/`, **Codex는 `.agents/skills/` 정션**(git 미추적, 기기별 `mklink /J`)
- **미완료 — 훅 2종 미설치. 이게 없으면 이 재편은 종전보다 나쁘다** (컴팩션 전량 생존을 버렸는데 강제력을 못 얻은 상태). 스크래치패드의 `commit_gate.py`(무차별 스테이징·대량 삭제·메시지 형식·정합성)와 `session_boot.py`(현황판 주입 + check.py 위임 + 스킬 라우팅 힌트)를 `.claude/hooks/`로 복사하고 `settings.json`의 `PreToolUse` `Bash|PowerShell` 매처에 `commit_gate.py` 등록 필요. 설치 전까지 커밋 전에 `python harness/check.py` 수동 실행
- 2026-08-15 비평 에이전트 평가 결과: **조건부 진행.** 미설치 상태 점수 46 < 단일 CLAUDE.md 300줄 방식 52, 설치·수정 후 54. 지적 12건 중 A-1(한글 name)은 Claude Code에서 반증됐으나 규격 위반은 사실이라 ASCII 슬러그로 전환 완료. 나머지 조치 완료, 헤더 8필드→3필드 축소만 오픈 후로 유예
- 다음: **GitHub 저장소(jjong-pp/Jarvis_pr)가 PUBLIC 상태 — private 전환 전까지 push 금지** (2026-08-07 확인). 단 2026-08-12 세션 커밋은 사용자가 push 완료

### 세일즈허브 (아이베 매출 대시보드)
- 상태: 2026-08-12 `handoff-eibe-sales-hub.zip`(08-07 작성분) 수신, `세일즈허브/` 폴더로 편입. 구글시트 기반 채널별 매출·행사ROI 대시보드, SSO 게이트(@eibe.co.kr·@btmall.kr)
- **[2026-08-15] 폴더 복원.** 커밋 84c7390(2026-08-14, 메시지 없음)이 20개 파일 8,129줄을 삭제했으나 사용자 확인 결과 의도된 삭제가 아니었음. `git checkout 84c7390^ -- 세일즈허브` 로 전량 복원. 룰은 삭제 후에도 이 폴더를 정본으로 가리키고 있었고, 이 사고가 `commit_gate.py` 도입 근거가 됨
- 미확인: 프로젝트 목적·배경, 개발 단계(완성/진행중), 실제 배포 여부, "handoff" 의미(인수 vs 전달 예정)
- 다음: 사용자에게 프로젝트 맥락 확인 → `.claude/skills/sales-hub/SKILL.md` 보완

### 관세청 API 연동 (뉴트리시아몰, GeekStudio 협업)
- **[2026-09-01 사용자 확정] 전송 구조·적용 예정:** 동수의 자이언트 전송 규격은 **V2-1로 통일**하고 Geek은 자사몰 주문의 보완 필드를 **V2-2**로 전송한다. 자이언트는 동일 `(hwbNo, orderNo)`의 중복 필드에서 Geek V2-2의 유효한 비공백 값을 우선하고, V2-2의 `null`·빈 문자열로 V2-1 유효값을 지우지 않으며 필드별 최종 출처를 로그로 남긴다. 동수의 물류·상품 고유 필드는 Geek이 임의로 덮지 않는다. 동수는 2026-09-01 개발 완료 후 **2026-09-02 주문 건부터 적용 예정**이나 개발 완료·실전 반영·E2E 성공은 아직 미확인이다. 원문 `v1 gate`는 보존하고 공유 문서 명칭은 V2-1로 통일한다.
- **[2026-08-31 발생] 90건 전환 사고:** 자이언트에는 구 API 데이터만 전달되어 신규 전자상거래 신고에 필요한 업체부호·일회용 인증번호 등 데이터가 전달되지 않았다. 90건을 신규 방식으로 신고하려면 2026-09-01 13:00~14:00 전까지 전체 필드를 제공해야 한다는 자이언트 회신이 있었으나, 실제 보완 완료 건수·신고 결과는 미확인이다.
- **PCCC 수집 오류:** 개인통관고유부호 미확보 주문은 데이터 수집 오류로 개발 로그에서 복구 후 수기 전달하기로 했다. 대상 건수, 복구 건수, 미복구 건수, 수기 전달 시각은 미확인이다. `O000000000004`는 형식은 O+12자리이나 자이언트가 임의값을 의심한 사례일 뿐, 진위 검증 결과는 아직 없다. 검증 전 “가짜”로 확정하지 않는다.
- **단기 검증 예외의 범위:** 동수 V2-1에 일회용 번호가 없을 수 있다는 질의에 자이언트는 “수하인정보 검증하고 개인통관고유부호는 제외”라고 답했다. 이는 해당 전환 구간의 자이언트 입력 검증 예외로만 기록하며, 관세 신고에서 PCCC가 법적·업무적으로 면제된다는 뜻으로 확대하지 않는다. 적용 대상·시작·종료·재처리 기준은 서면 확정이 필요하다.
- **신고 방식 혼재:** 기존 신고와 전자상거래 신고가 2026-08-31부터 “향후 3일” 혼재될 예정이라고 공유되었다. 정확한 종료 일시와 주문별 판정 필드는 대화만으로 확정할 수 없으므로 각 주문에 `declarationMode`, 판정 근거, 판정 주체, 판정 시각을 남겨야 한다. A/B 거래 역할과 신고 경로는 서로 추론하지 않는다.
- **유지되는 관세청 기준·채널 구조:** TRA001 거래정보 제출 주체는 쇼핑몰 운영 전자상거래업자이며 관세사·특송사의 수입신고 전자문서와 구분한다. TRA001 `su_crtf_no`는 명세상 선택값(N)이고 기존 수입신고는 TRA001·OTP가 불필요하나, 전자상거래 전용 신고와 자이언트 API의 필수조건은 별도 계약이므로 임의 OTP·`Z99999`·가짜 완료일시를 만들지 않는다. 채널은 총 11개(자사몰 A 1 + Naver B 1 + 기타 외부 B 9)이며 외부 플랫폼 B의 제출·인증·결과 제공 범위는 채널별로 확정한다. Naver는 2026-10-14 관세청 연동 시작 예정이고 세부 API·필드·OTP·결과 공개 범위는 서면 확인 전 확정하지 않는다.
- **즉시 확인할 P0:** 자이언트가 ① V2-2 우선·빈값 보호를 적용했는지 ② 90건별 최종 신고 방식·필드 보완·접수 결과가 무엇인지 ③ PCCC 검증 제외의 정확한 범위와 종료 조건이 무엇인지 ④ `accepted/pending/merged`와 필드 출처 로그를 제공할 수 있는지 확인한다. 동수는 2026-09-02 첫 주문의 V2-1 요청·응답·키·재시도 로그를 제공하고, Geek은 동일 키 V2-2 보완 필드와 인증 원본 출처를 대사한다.
- **정본·원본:** 현재 브리핑은 `관세청/planning/현황브리핑_20260901.html`, 전체 정본은 `관세청/planning/리스크_질의대장.html`, 공유 설계는 `관세청/planning/planning_v6.html`, 필드 계약은 `관세청/planning/api_field_mapping.html`, 자이언트 메모는 `관세청/documents/자이언트_API_메모.md`, 2026-09-01 대화 원문은 `관세청/documents/20260901_자이언트_운영전환_협의원문.md`, 관세청 원본은 `관세청/documents/*.pdf`다.

### 블로그
- 상태: 하네스 엔지니어링 3부작 초안 완료 (`blog/`)
- 다음: 퇴고 및 발행

### 이력서/구직
- 정본 교체: `resume/career_facts.md` 신설(claim ID·검증상태·트랙·미해결질문 9건). 종전 정본 2종은 작업 트리에서 삭제돼 git 이력에만 존재. `resume` 스킬의 죽은 파일 지도 12건 정리 완료.
- 하네스 구축: 파이프라인 4단계(`jd-analyst` → `fit-strategist` → `application-writer` → `application-auditor`)와 루프 규칙 6개를 `resume` 스킬에 등재. 에이전트 정의 4종 신설.
- 포지셔닝: **이중 트랙** 확정(L=대장·검증 / P=제품·성장). 진단은 `resume/포트폴리오_공고_적합성_진단_2026-08-29.md`.
- 폴더 1정본화(2026-08-30): 루트에 `career_facts.md`만. 공고 자료는 `applications/{공고}/`, 과거 분석은 `_archive/`.
- 확정: B2B 일정 `06 기획/07.02 착수/07.16 전체가동/08.06~13 고도화/08.31 최종검수`, 관세청 시작 `2026.02`, 오출고 전체 측정범위. 간략소개 3곳 공용 정본 확정.
- 타겟 3사: 리얼티쓰(이력서 초안 완료) · 더페어글로벌 · 원더무브. 제출 형식은 이력서 + 포트폴리오 + 마켓빅시 정책 PDF 동시 제출. 사람인은 입사후포부·성공실패사례 추가.
- 미해결: BIGSEE 등록 20명 vs 로그인 180명 모수 충돌 → **22장본 21장 재계산 또는 삭제 판단 대기.** BIGSEE 시작월, 병역, 마켓빅시 고용형태 등 5건.
- 다음: 22장본 수정 15건 반영, 리얼티쓰 자소서, 더페어·원더무브 이력서.

## 보류 / 완료
- 세컨드브레인 볼트 재구축 (2026-08-06 완료) — Antigravity용 자동화를 Claude Code 네이티브 구조로 교체
