@AGENTS.md

## Claude Code 어댑터

위 `AGENTS.md`가 도구 무관 헌법이다. Codex 등 다른 에이전트도 같은 파일을 읽는다.
아래는 Claude Code에서만 의미가 있는 것이다. 하네스 규격 전체는 `harness/SPEC.md`.

**서브에이전트** — `researcher` 외부 조사(소스/요약/시사점) · `reviewer` 확정 전 리스크 검토(리스크마다 대응책) · `organizer` 폴더 정리 + 현황판 갱신.
외부 조사·확정 전 검토·사용자가 명시적으로 요청한 경우에만 부른다. **서브에이전트는 이 파일을 상속받지 않는다** — 위임할 때 필요한 사실을 프롬프트에 직접 넣는다. 에이전트 보고는 액면 그대로 받지 않고 원본과 대조한 뒤 반영한다.

**커맨드** — `/정리` 세션 마무리 · `/검토 <대상>` 확정 전 검토 · `/압축` L1 비대화 해소.

**하네스 수정** — L1·L2 파일과 에이전트 정의 수정은 메인 에이전트만, **증분 수정으로만** 한다. 전체 재작성은 사용자 승인이 필요하다. 병렬 수정으로 규칙이 깨진 전례가 있다.
