# MyMain 작업공간 규칙

기획 · 소규모 개발 · 사무업무(SCM, 운영 관리 등)를 하나의 작업공간에서 처리하기 위한 규칙.
규칙은 짧고 단순하게 유지한다 — 늘어나면 합치거나 지운다. 기본 응답 언어는 한국어.

## 1. 폴더 지도
- `관세청/` — 전자상거래 통관 API 연동 프로젝트 (기획·개발·계약 문서)
- `산후조리원/` — 고도몰 쇼핑몰 구축·운영 (체크리스트, 견적)
- `blog/` — 블로그 원고
- `resume/` — 이력서·지원 서류
- `00_Inbox` ~ `04_Archive` — PARA 노트 볼트 (아이디어·지식 정리용, `README.md` 참고)
- `project_state.md` — 전체 프로젝트 현황판 (아래 2번)

## 2. 세션 시작 (기억 복원)
새 세션의 첫 응답 전에 `project_state.md` **하나만** 읽는다. 폴더 전체 스캔 금지.
작업으로 프로젝트 상황이 바뀌면 그 자리에서 해당 항목만 갱신한다 (증분 수정, 마지막 갱신일 기록).

## 3. 일 처리 방식 (3단계 게이팅)
1. **가벼운 일** (질문, 단순 수정, 현황 확인) → 바로 처리. 에이전트·절차 없음.
2. **보통 일** (문서 초안, 코드 작성, 정리) → 아래 4번 업무 모드 규칙에 따라 메인이 직접 처리.
3. **무거운 일** (외부 조사 필요, 확정 전 검토 필요, 사용자가 명시적으로 요청) → 서브에이전트 투입 (아래 5번).

판단이 애매하면 가벼운 쪽을 택한다. 목표나 대상 파일이 불명확하면 실행 전에 한 번 확인한다.

## 4. 업무 모드
- **기획**: 결과물은 마크다운 초안 → 사용자 확인 → 확정. [배경/목표/실행안/리스크/다음 행동] 구조를 기본으로.
- **개발(소규모)**: 기존 파일·패턴 먼저 확인 후 최소 수정. 값을 코드에 박지 말고 설정/플래그로 (Zero-Hardcoding). 완료 후 변경 내용 3줄 요약.
- **사무**: 표와 체크리스트 중심. 날짜는 절대 날짜(YYYY-MM-DD). 금액·수치·계좌는 반드시 원본 문서에서 재확인 후 기재.

## 5. 서브에이전트 (`.claude/agents/`)
| 이름 | 역할 | 언제 쓰는가 |
|---|---|---|
| `researcher` | 외부 자료 조사 — [소스/요약/시사점] 구조로 보고 | 외부 정보·최신 동향·사례가 필요할 때 |
| `reviewer` | 리스크·허점 검토 — 각 리스크에 대응책을 짝지어 제시 | 기획서·계약·견적·코드를 확정하기 전 |
| `organizer` | 산출물을 PARA 볼트에 정리, `project_state.md` 갱신 | 세션 마무리, "정리해줘" 요청 시 |

## 6. 룰 관리 원칙
- 이 파일과 에이전트 정의 수정은 메인 에이전트만, 증분 수정으로만. 전체 재작성은 사용자 승인 필요.
- 수정 전 상태는 git 커밋으로 남긴다.
- 같은 실수를 두 번 지적받으면, 해당 업무 모드 항목에 방지 규칙을 1줄로 추가한다.

## 7. 안전장치 (자동 훅)
`.claude/hooks/`의 훅이 자동으로 차단한다:
- 파괴적 명령: 광범위 `rm -rf`, `git reset --hard`, `git clean -f`, 강제 push, `--no-verify`
- 보호 파일 수정: 시크릿(.env/키 파일), 원본 문서(PDF/HWP), 읽기 전용 레퍼런스 `룰 참고용/`

차단 메시지가 뜨면 우회하지 말고 사용자에게 알린다.
