# 학습 로그

실수와 교정, 그리고 통한 방식을 기록한다. 발생 즉시 한 줄 추가한다.
**회전 규칙**: 각 절 10건을 넘기면 오래된 것부터 `.claude/archive/학습로그_YYYYMM.md`로 옮긴다. 이 파일은 매 세션 로드되므로 짧게 유지한다.

형식: `- [YYYY-MM-DD] 무엇이 잘못됐는가 → 다음부터 어떻게 (근거·출처)`

## 오답 노트

- [2026-08-11] 룰을 폴더별 `CLAUDE.md`로 분산했다가 되돌림. 하위 폴더 룰은 그 폴더 파일을 읽을 때만 로드되고 컴팩션 후 재주입되지 않는다 → 로딩 방식을 공식 문서로 확인한 뒤 구조를 정한다 (출처: code.claude.com/docs/en/memory).
- [2026-08-11] `project_state.md`가 `risk_list.html`보다 4일 뒤처져, 이미 해결된 리스크(PER001 결제 차단)를 최대 리스크로 유지하고 있었음 → 현황판과 정본 문서의 갱신일을 비교하는 점검을 `/정리`에 넣음.
- [2026-08-11] 영한 병기(`대장(ledger)`)를 사용해 커뮤니케이션 규칙을 위반 → 이모지·영한 병기 금지, 건조한 팩트 문장.

## 성공 패턴

- [2026-08-11] 구조를 정하기 전에 공식 문서로 로딩 동작을 검증하고 근거와 함께 권고안을 제시한 방식이 통했다. 추측으로 아키텍처를 정하지 않는다.
- [2026-08-11] 작업 시작 전 `AskUserQuestion`으로 목적·구조·작업방식·축적방식 네 축을 먼저 확정한 뒤 만든 결과물이 재작업 없이 통과했다.
