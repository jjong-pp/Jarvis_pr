# 학습 로그

실수와 교정, 그리고 통한 방식을 기록한다. 발생 즉시 한 줄 추가한다.
**회전 규칙**: 각 절 10건을 넘기면 오래된 것부터 `.claude/archive/학습로그_YYYYMM.md`로 옮긴다. 이 파일은 매 세션 로드되므로 짧게 유지한다.

형식: `- [YYYY-MM-DD] 무엇이 잘못됐는가 → 다음부터 어떻게 (근거·출처)`

## 오답 노트

> 2026-08-11 건 3건은 `.claude/archive/학습로그_202608.md`로 이관 (2026-08-15, 회전 규칙).

- [2026-08-12] `project_state.md`에 룰 파일 경로를 `관세청/.claude/rules/20_관세청.md`로 잘못 적음(실제는 루트 `.claude/rules/`) → 룰 파일 경로를 인용할 때 실제 위치를 다시 확인한다.
- [2026-08-12] "SCM·MD 운영"을 PM/기획·개인자산관리와 같은 급의 별도 업무유형으로 만듦 → 실제로는 업무모드(기획/개발/사무/외부조사) 4가지로 이미 충분했고, SCM/물류 지식은 일하는 방식이 아니라 여러 프로젝트가 참조하는 도메인 지식이었다. 업무유형을 늘리기 전에 "이게 새로운 공정 방식인가, 아니면 재사용되는 지식인가"부터 구분한다 (사용자 교정, `40_SCM물류_전문지식.md`로 분리).

- [2026-08-13] 중문 공유본에 폐기된 지침 5건(OTP 동수 전달, 단가 2자리 고정, 중량 ±5~10% 허용, 국내건 제외, `sellerName` 전건 Null)이 살아 있어 협력사가 그대로 개발하면 재작업이 날 상태였다 → **국문을 정정할 때 중문에서 "값이 같은지"가 아니라 "폐기된 옛 문구가 죽었는지"를 확인한다.** 정정은 삭제가 아니라 `原…废止` 형식으로 남긴다.
- [2026-08-13] 문서 통합(`risk_list.html`·`협력사_질의응답.html` → `리스크_질의대장.html`)을 하면서 룰 파일의 정본 지정·파일 지도와 `project_state.md`를 같이 고치지 않아, 룰이 존재하지 않는 파일을 정본으로 가리키고 현황판이 이미 해소된 P0를 유지했다 → **문서를 삭제·통합하는 커밋에는 그 문서를 가리키는 룰·현황판 수정을 반드시 포함한다** (CLAUDE.md 0번 3·4번 사고 유형).

- [2026-08-15] 08-13에 "삭제 커밋에는 룰 수정을 포함한다"를 이 파일에 적었는데 **다음 날 그대로 재발**했다(커밋 84c7390이 세일즈허브 20개 파일 8,129줄을 삭제, 룰은 계속 그 폴더를 정본으로 지목). 원인은 의지가 아니라 구조였다 — 상시 로드 룰이 597줄이었고 모델이 안정적으로 지키는 지시는 150~200개인데 시스템 프롬프트가 이미 ~50개를 쓴다. **룰을 늘리면 기존 룰의 준수율이 같이 떨어진다.** → 같은 실수를 두 번 지적받으면 문장을 세 번째로 쓰지 말고 L0 훅으로 올린다. `commit_gate.py`가 정본 경로 실재 여부를 커밋 시점에 검사한다.
- [2026-08-15] 커밋 메시지가 전체 20건 중 12건 `no message`. 형식 규칙이 문서에 있었으나 강제되지 않았다 → `commit_gate.py`가 `YYYYMMDD-설명` 형식을 차단으로 강제. **문서에만 있는 규칙은 규칙이 아니라 희망이다.**
- [2026-08-15] "온디맨드 로딩은 컴팩션에서 유실되므로 전량 상시 로드밖에 없다"고 2026-08-11에 결론냈으나 **틀렸다.** 하위 `CLAUDE.md`와 `paths:` 룰은 유실되지만 **호출된 스킬 본문은 재주입된다**(스킬당 5,000토큰 상한, 앞쪽 보존). 두 선택지만 놓고 판단한 것이 원인 → 구조를 결정하기 전에 **가능한 메커니즘을 전부 나열했는지** 먼저 확인한다.
- [2026-08-15] `market-bigsee/AGENTS.md`(129파일 프로젝트의 운영 규칙)가 Claude 세션에서 로드된 적이 없었다. AGENTS.md는 Codex 계열 규약이고 **Claude Code는 CLAUDE.md만 읽는다** → 다른 도구용 설정 파일을 두면 각 도구가 실제로 그 파일을 읽는지 공식 문서로 확인한다. 루트는 `@AGENTS.md` 임포트, 하위 폴더는 스킬 등록으로 해결.

- [2026-08-15] **자기가 만든 방어를 자기가 검증하면 놓친다.** D1을 막겠다고 만든 커밋 게이트가 정작 README·커맨드·organizer에 생긴 같은 유형 dangling 4건을 통과시켰고, 게이트 자신에 `--allow-empty-message` 우회가 있어 D4도 못 막았다. 비평 에이전트를 붙여서야 나왔다 → **하네스를 고치면 반드시 외부 검증을 한 번 태운다.** 다만 에이전트 보고도 액면 그대로 받지 않는다 — P0 1건이 반증됐고 1건은 과소 집계였다.
- [2026-08-15] Git Bash `sed -i` 로 한글 파일을 편집해 `blog/SKILL.md` 인코딩이 깨졌다(cp949 혼입) → **한글이 든 파일은 `sed`·`awk` 인플레이스 편집 금지.** Edit/Write 도구를 쓴다. 일괄 치환이 필요하면 Python에서 `encoding='utf-8'` 명시로 읽고 쓴다.
- [2026-08-20] API 대화 정리에서 질의자 Geek과 마지막 질문자 동수를 구분하지 않고 모두 "상대방"으로 뭉뚱그렸다 → 메신저 대화는 발화자·질문·답변을 먼저 대응시킨 뒤 기술적 쟁점을 해석한다.
- [2026-08-24] 관세청 확인으로 자사몰은 A만 운영한다고 확정됐는데 플랫폼 B 흐름과 섞어 자사몰 B 가능성을 반복 제시했다 → 유형을 설명할 때 일반 규격보다 먼저 채널별 확정 운영값(자사몰 A, 네이버·쿠팡 B)과 제출 주체를 고정한다.
- [2026-08-24 정정] 플랫폼을 네이버·쿠팡만으로 축소하면서 기존 사업자 분기(네이버스토어만 AIBE, 기타 외부 채널 KRONE)를 삭제했다 → 채널 수와 사업자 패턴을 별개 축으로 보존한다: 자사몰=A/KRONE, 네이버스토어=B/AIBE, 기타 외부 27개=B/KRONE.
- [2026-08-26] 협력사(Geek)가 **구버전 `planning_v6.html`** 기준으로 TRA001을 개발해 운영에서 실오류가 났다(`sale_ents_nm`·`sale_ents_sgn`이 옛 버전에서 필수가 아니어서 제외 처리됨). 규격 오해가 아니라 **버전 드리프트**였고, 파일명에 배포일자가 없어 서로 어느 판을 보는지 확인할 수 없었다 → **공유본 파일명에 배포일자를 넣고, 재배포 시 변경 필드 델타를 함께 준다.** 값을 정정할 때는 "우리 문서가 맞는지"가 아니라 **"상대가 들고 있는 판이 무엇인지"**를 먼저 묻는다.
- [2026-08-26] 기획서의 자이언트 Postman 링크가 **구판(`2sBXqGrh3A`)을 가리킨 채 방치**돼, v2.0.0(08-20)에서 `PATCH /api/hwb/{hwbNo}`·PCCC 검증·레거시 `search`가 **삭제**되고 `HWB 수정`이 “삭제 예정”이 된 것을 6일 뒤에야 발견했다. 기획서는 그동안 존재하지 않는 PATCH로 정정하는 시나리오를 유지하고 있었다 → **외부 API는 링크만 적어두지 말고 버전·대조일을 함께 적고, 규격을 인용할 때마다 컬렉션 원본 JSON을 다시 받아 changelog부터 읽는다.** 화면이 아니라 JSON이고, 필드가 아니라 changelog가 먼저다.
- [2026-09-04] 고도몰 게시판 HTML을 채팅 코드블록으로 줄이면서 존재하지 않는 이미지 URL을 실제 경로처럼 넣어 깨진 이미지를 만들었다 → 실제 이미지 URL을 확인하지 못했으면 임의 URL을 만들지 않고, 검증된 Base64 데이터 또는 사용자가 업로드한 실제 주소만 사용한다 (사용자 교정, 공지 `sno=3`).

## 성공 패턴

- [2026-08-11] 구조를 정하기 전에 공식 문서로 로딩 동작을 검증하고 근거와 함께 권고안을 제시한 방식이 통했다. 추측으로 아키텍처를 정하지 않는다.
- [2026-08-11] 작업 시작 전 `AskUserQuestion`으로 목적·구조·작업방식·축적방식 네 축을 먼저 확정한 뒤 만든 결과물이 재작업 없이 통과했다.
- [2026-08-12] 하네스 자기보호 훅이 실제로 작동함을 실증. `.claude/hooks/session_boot.py`를 Write로 수정 시도 → 차단됨. Bash `cp`로 우회 가능했으나 하지 않고 사용자에게 명령을 넘겼다 — 차단을 우회하면 그 보호의 존재 의미가 사라진다 (`CLAUDE.md` 8번).
